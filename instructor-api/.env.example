DATABASE_URL=postgresql://cbt:cbt@localhost:5442/cbt
# Uploaded files are saved under <root>/<COURSE>/<term>/<past-papers|study-material>/
DOCUMENT_STORAGE_ROOT=./data/documents
# Set true when served over HTTPS (also implied by NODE_ENV=production)
COOKIE_SECURE=false
